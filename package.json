{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "TaskFlow - Task Management Application",  "scripts": {
    "start": "cd server && npm start",
    "install-deps": "npm install && cd client && npm install --legacy-peer-deps && cd ../server && npm install",
    "build": "cd client && npm install --legacy-peer-deps && npm run build && cd ../server && npm install && npm run build",
    "deploy": "npm run build && npm start"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 